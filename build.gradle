group 'org.time2die'
version '1.0'

apply plugin: 'idea'

subprojects {
    buildscript {
        repositories {
            mavenCentral()
            mavenLocal()
        }
    }
}


project(':client') {
    apply plugin: 'application'
    apply plugin: 'scala'
}

